import React from 'react';
import { createRoot } from 'react-dom/client';
import CollectionApp from './CollectionApp';

createRoot(document.getElementById('root')!).render(React.createElement(CollectionApp));
